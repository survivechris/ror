<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!--now the render's directory is searching at /articles instead of /layouts, so we should make it search the /layouts directory-->
    <%= render 'layouts/navigation'%>
    <%= render 'layouts/messages'%>
    <div class=container>
    <%= yield %>
    </div>
    <%= render 'layouts/footer'%>
  </body>
</html>
